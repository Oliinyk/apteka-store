services:
  - type: web
    name: vita-store
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 22.11.0
      - key: ADMIN_USER
        sync: false
      - key: ADMIN_PASSWORD
        sync: false
